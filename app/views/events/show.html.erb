<div class="max-w-4xl mx-auto bg-white p-6 shadow-md rounded-lg">
  <h1 class="text-3xl font-bold text-center mb-4"><%= @event.title %></h1>

  <p class="text-gray-600 text-center mb-4"><%= @event.description %></p>

  <div class="grid grid-cols-2 gap-4 text-center">
    <div>
      <strong>📅 Fecha de Inicio:</strong>
      <p><%= @event.start_time.strftime("%d/%m/%Y %H:%M") %></p>
    </div>
    <div>
      <strong>📅 Fecha de Fin:</strong>
      <p><%= @event.end_time.strftime("%d/%m/%Y %H:%M") %></p>
    </div>
  </div>

  <div class="mt-4 text-center">
    <strong>🏷️ Etiquetas:</strong>
    <p class="text-gray-500"><%= @event.labels.map(&:name).join(", ") %></p>
  </div>

  <div class="mt-6 flex justify-center gap-4">
    <% if current_user.events.include?(@event) %>
      <%= button_to "❌ Desinscribirse", unregister_event_path(@event), method: :delete, class: "bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-md transition" %>
    <% else %>
      <%= button_to "✅ Inscribirse", register_event_path(@event), method: :post, class: "bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded-md transition" %>
    <% end %>

    
  </div>
</div>

